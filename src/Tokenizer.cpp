#include "Tokenizer.h"

// Toma una linea de datos y la separa por palabras, guardando tambien su valor.
TLineaDato tokenizeDato(char *buffer) {
    return TLineaDato();
}

// Toma una linea de texto y la separa por palabras.
vector<TOracion> Tokenizer::tokenizeTest(char *buffer) {
    return vector<TOracion>();
}
