#include "Tokenizer.h"

// Toma una linea de datos y la separa por palabras, guardando tambien su valor.
TLineaDato tokenizeDato(string buffer) {
    return TLineaDato();
}

// Toma una linea de texto y la separa por palabras.
TLineaTest Tokenizer::tokenizeTest(string buffer) {
    return TLineaTest();
}
